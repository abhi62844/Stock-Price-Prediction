1. install tensorflow, matplotlib, pandas
pip install yfinance
pip install tensorflow

2. run dataset.py to fetch the latest data, and save it in a csv file. (the dataset is converted to heikin ashi form)
python dataset.py
3. open and run all cell in app.ipynb, 
